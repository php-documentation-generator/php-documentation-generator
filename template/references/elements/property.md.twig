{% block property %}
### <a href="#property-{{ property.name }}" id="property-{{ property.name }}">§</a> {{ property.modifier }}{% if property.hasType %} {{ property.type|md_link|raw }}{% endif %} ${{ property.name }}{% if property.hasDefaultValue and property.defaultValue|length > 0 %} = `{{ property.defaultValue|md_value }}`{% endif %}

{% if property.docComment|md_sanitize|raw|length > 0 %}

{{ property.docComment|md_sanitize|raw }}
{% endif %}
{% if property.additionalTypes %}

Types:

> {{ property.additionalTypes|md_link|raw }}
{% endif %}
{% if property.accessors|length > 0 %}

**Accessors**: `{{ property.accessors|map(accessor => accessor.name)|join('`, `') }}`
{% endif %}
{% endblock %}
